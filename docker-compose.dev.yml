# Docker Compose for Development
# 默认：只运行数据库服务，应用代码在本地运行
# prod-test 模式：运行完整生产镜像测试（先构建镜像再用 --profile prod-test 启动）
#   docker build -f Dockerfile.multi --target api-build -t librechat-local:test .
#   docker compose -f docker-compose.dev.yml --profile prod-test up

services:
  mongodb:
    container_name: chat-mongodb
    image: mongo:8.0.17
    restart: always
    ports:
      - "27017:27017"  # 暴露给本地访问
    volumes:
      - ./data-node:/data/db
    command: mongod --noauth

  meilisearch:
    container_name: chat-meilisearch
    image: getmeili/meilisearch:v1.12.3
    restart: always
    ports:
      - "7700:7700"  # 暴露给本地访问
    environment:
      - MEILI_HOST=http://meilisearch:7700
      - MEILI_NO_ANALYTICS=true
      - MEILI_MASTER_KEY=${MEILI_MASTER_KEY:-your-meilisearch-master-key-here}
    volumes:
      - ./meili_data_v1.12:/meili_data

  vectordb:
    container_name: vectordb
    image: pgvector/pgvector:0.8.0-pg15-trixie
    ports:
      - "5433:5432"  # 使用 5433 避免与本地 PostgreSQL 冲突
    environment:
      POSTGRES_DB: mydatabase
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: mypassword
    restart: always
    volumes:
      - pgdata2:/var/lib/postgresql/data
    profiles:
      - rag  # 标记为可选服务

  rag_api:
    container_name: rag_api
    image: ghcr.io/danny-avila/librechat-rag-api-dev-lite:latest
    ports:
      - "8000:8000"  # 暴露给本地访问
    environment:
      - DB_HOST=vectordb
      - RAG_PORT=${RAG_PORT:-8000}
    restart: always
    depends_on:
      - vectordb
    env_file:
      - .env
    profiles:
      - rag  # 标记为可选服务

  # 生产镜像本地测试服务（--profile prod-test 启用）
  # 先构建：docker build -f Dockerfile.multi --target api-build -t librechat-local:test .
  api-prod-test:
    container_name: LibreChat-prod-test
    image: librechat-local:test
    restart: always
    ports:
      - "3080:3080"
    depends_on:
      - mongodb
    env_file:
      - .env
    environment:
      - HOST=0.0.0.0
      - PORT=3080
      - MONGO_URI=mongodb://mongodb:27017/LibreChat
      - MEILI_HOST=http://meilisearch:7700
      - DOMAIN_CLIENT=http://localhost:3080
      - DOMAIN_SERVER=http://localhost:3080
    volumes:
      - ./.env:/app/.env
      - ./librechat.yaml:/app/librechat.yaml
      - ./images:/app/client/public/images
      - ./uploads:/app/uploads
      - ./logs:/app/logs
      - ./data:/app/data
      - ./data:/app/api/data
    profiles:
      - prod-test

volumes:
  pgdata2:
